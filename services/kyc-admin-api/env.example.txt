# =====================================================
# TA-Edu KYC Admin API - Mẫu biến môi trường
# =====================================================
# Hướng dẫn khi clone repo về máy mới:
#   1. Copy file này và đổi tên thành .env
#      (PowerShell): Copy-Item env.example.txt .env
#      (macOS/Linux): cp env.example.txt .env
#   2. Điền các giá trị thật vào chỗ có <...>
#   3. KHÔNG đẩy file .env lên GitHub
# =====================================================


# ---- Database ----------------------------------------
# Khi chạy qua docker-compose, giá trị này được ghi đè tự động.
# Chỉ cần đổi nếu bạn chạy API bên ngoài Docker.
DATABASE_URL=postgres://taedu:taedu@localhost:5434/taedu

# Kích thước vector — phải khớp model embedding (MiniLM local = 384). Chỉ đổi nếu đổi model.
# EMBEDDING_DIM=384


# ---- API Server --------------------------------------
PORT=4001


# ---- JWT (bắt buộc đổi khi deploy production) -------
JWT_SECRET=super-secret-change-me
JWT_TTL=8h


# ---- Admin mặc định (seed khi boot lần đầu) ---------
DEFAULT_ADMIN_USERNAME=admin
DEFAULT_ADMIN_PASSWORD=admin123


# ---- CORS --------------------------------------------
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5000


# ---- Firebase ----------------------------------------
# Project ID: xem tại Firebase Console > Project Settings
FIREBASE_PROJECT_ID=<your-firebase-project-id>

# Service Account - chọn 1 trong 2 cách bên dưới:
# Cách 1 (Docker/CI): dán toàn bộ JSON vào 1 dòng
FIREBASE_SERVICE_ACCOUNT=<json-string-cua-service-account>
# Cách 2 (local dev): trỏ đến file JSON đã tải từ Firebase Console
# GOOGLE_APPLICATION_CREDENTIALS=./serviceAccountKey.json


# ---- Google Gemini AI --------------------------------
# Lấy API key: https://makersuite.google.com/app/apikey
# Bắt buộc nếu muốn Tạo bài tập / đề KTPL do AI sinh (POST /api/exam/exercise/generate).
# Tư vấn luật: nếu trống vẫn tra cứu nội bộ; tạo bài tập sẽ trả bản minh họa (mock).
# Trên VPS: đặt trong file này rồi docker compose up -d --force-recreate kyc-admin-api
GEMINI_API_KEY=<your-gemini-api-key>

# Tuỳ chọn — model Gemini (mặc định gemini-2.5-flash). Nếu tạo bài tập lỗi, thử: GEMINI_MODEL=gemini-2.0-flash
# GEMINI_MODEL=gemini-2.5-flash


# ---- SMTP (email thông báo - tùy chọn) --------------
# Chỉ cần nếu dùng tính năng gửi email thông báo phụ huynh
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=<your-email@gmail.com>
SMTP_PASS=<your-gmail-app-password>
SMTP_FROM=TA-Edu <no-reply@ta-edu.local>

